$.fn.serializeObject = function () {
    "use strict";
    var a = {}, b = function (b, c) {
        var d = a[c.name];
        var v = c.value;
        isNaN(Number(v)) ? v = v : v = parseInt(v);
        "undefined" != typeof d && d !== null ? $.isArray(d) ? d.push(v) : a[c.name] = [d, v] : a[c.name] = v
    };
    return $.each(this.serializeArray(), b), a
};